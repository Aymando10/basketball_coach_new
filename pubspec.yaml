name: basketball_coach
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.0.0
  cupertino_icons: ^1.0.8
  http: ^1.5.0
  camera: ^0.11.0+2
  path: ^1.9.0
  path_provider: ^2.0.11
  image: ^4.0.21
  http_parser: ^4.1.2
  youtube_player_iframe: ^5.2.2
  google_mlkit_pose_detection: ^0.10.0
  google_mlkit_commons: ^0.6.0
  sqflite: ^2.3.0
  permission_handler: ^12.0.1
  fl_chart: ^0.70.0
  url_launcher: ^6.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/feedback_rules.json
